[project]
name = "github-visual-primitives-gaia"
version = "0.1.0"
description = "Gaia formalization of DeepSeek Visual Primitives -- Thinking with Visual Primitives (DeepSeek-AI + Peking University + Tsinghua University, 2026-04-30 GitHub release, withdrawn)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.11,<0.9.0"]
build-backend = "uv_build"

[tool.hatch.build.targets.wheel]
packages = ["src/github_visual_primitives"]

[tool.gaia]
type = "knowledge-package"
uuid = "1f16acb0-cbc2-4f47-872d-2217c45c7942"
